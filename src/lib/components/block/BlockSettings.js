import React from 'react';

export default ({ }) => {
    return <div className="block-settings">
        <label>
            Form:
            <input type="checkbox" name="isForm" />
        </label>
    </div>
}